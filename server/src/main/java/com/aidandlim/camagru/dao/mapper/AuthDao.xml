<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aidandlim.camagru.dao.AuthDao">

    <select id="signin" resultType="com.aidandlim.camagru.dto.User">
        SELECT * FROM
            user
        WHERE
            email = #{email} AND password = PASSWORD(#{password})
    </select>

    <insert id="signup">
        INSERT INTO user
            (email, password, nickname)
        VALUES
            (#{email}, PASSWORD(#{password}), #{nickname})
    </insert>

    <select id="selectById" resultType="com.aidandlim.camagru.dto.User">
        SELECT
            id, email, nickname, bio
        FROM
            user
        WHERE
            id = #{id}
    </select>

    <update id="update">
        UPDATE
            user
        SET
            email = #{email},
            nickname = #{nickname},
            bio = #{bio}
        WHERE
            id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE
            user
        SET
            password = PASSWORD(#{change})
        WHERE
            email = #{email}
    </update>

</mapper>